from distutils.core import setup
import setuptools


setup(
    name='pypipe',
    version='1.0',
    install_requires=[
    ],
    packages=[
        'pypipe'
    ],
    entry_points={
        "console_scripts": [
            ""
        ]
    }
)
